name: pipeline-with-secret
# This fixture contains a synthetic GitHub PAT placeholder for anti-leak testing.
# The token below is a SYNTHETIC PLACEHOLDER — not a real credential.

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Authenticate (insecure hardcoded token for test)
        run: |
          docker login ghcr.io \
            --username ci-bot \
            --password ghp_EXAMPLEsyntheticNOTREALaaaaaaaaaaaaaaa
